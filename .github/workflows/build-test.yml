name: Build 
run-name: Building Test
on:
  push:
    branches:
      - dev
jobs:
  Build-Portfolio-Test:
    env:
      SERVER: preview
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: '20.19.1'
      - run: npm install && npm run build
